# Digital Geoff - Zero Cost Requirements
#
# Minimal dependencies for free operation

# Web framework
fastapi>=0.109.0
uvicorn>=0.27.0
httpx>=0.26.0

# LLM clients
anthropic>=0.40.0  # Fallback only
openai>=1.12.0     # For Groq (OpenAI-compatible)

# Local storage (replaces paid services)
chromadb>=0.4.22   # Vector store (local)
# No external database needed - SQLite is built into Python

# Telegram bot
python-telegram-bot>=20.7

# Microsoft 365 (optional, free with subscription)
msal>=1.26.0
msgraph-sdk>=1.2.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Async support
aiofiles>=23.2.1

# Date handling
python-dateutil>=2.8.2
